{
  "name": "@iqprotocol/account",
  "version": "0.10.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test --group=unit",
    "prepublish": "npm run build"
  },
  "main": "dist/index.js",
  "module": "dist/account.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "README.md",
    "dist"
  ],
  "publishConfig": {
    "access": "restricted"
  },
  "dependencies": {
    "@iqprotocol/abstract-blockchain": "^0.8.0",
    "@iqprotocol/abstract-storage": "^0.8.0",
    "@iqprotocol/energy": "^0.5.0",
    "eth-sig-util": "^3.0.1"
  },
  "devDependencies": {
    "@iqprotocol/eip155": "^0.12.0"
  }
}
