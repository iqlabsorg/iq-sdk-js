{
  "name": "@iqprotocol/dvp",
  "version": "0.1.0",
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test",
    "prepublish": "npm run build"
  },
  "main": "dist/index.js",
  "module": "dist/dvp.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "README.md",
    "dist"
  ],
  "dependencies": {},
  "publishConfig": {
    "access": "public"
  }
}
