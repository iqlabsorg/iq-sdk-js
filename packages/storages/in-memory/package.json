{
  "name": "@iqprotocol/in-memory-storage",
  "version": "0.2.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test --group=unit",
    "prepublish": "npm run build"
  },
  "main": "dist/index.js",
  "module": "dist/in-memory-storage.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "README.md",
    "dist"
  ],
  "publishConfig": {
    "access": "restricted"
  },
  "dependencies": {
    "@iqprotocol/abstract-storage": "^0.2.0"
  }
}
