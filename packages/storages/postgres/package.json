{
  "name": "@iqprotocol/postgres-storage",
  "version": "0.11.0",
  "license": "UNLICENSED",
  "scripts": {
    "start": "tsdx watch --tsconfig tsconfig.build.json --verbose --noClean",
    "build": "tsdx build --tsconfig tsconfig.build.json",
    "test": "tsdx test --group=unit",
    "test:integration": "tsdx test --group=integration",
    "prepublish": "npm run build"
  },
  "main": "dist/index.js",
  "module": "dist/postgres-storage.esm.js",
  "typings": "dist/index.d.ts",
  "files": [
    "README.md",
    "dist"
  ],
  "publishConfig": {
    "access": "restricted"
  },
  "dependencies": {
    "@iqprotocol/abstract-storage": "^0.10.0",
    "slonik": "^24.0.0"
  }
}
